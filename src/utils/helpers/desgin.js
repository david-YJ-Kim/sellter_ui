// [].join(" "),

const btn = {
  text: ["px-4", "py-2", "text-lg", "font-semibold", "text-white"].join(" "),
};
const label = {
  text: ["text-sm", "font-semibold", "text-gray-500"].join(" "),
};

const state = {
  emphasis: ["focus:outline-none", "focus:ring-blue-200"].join(" "),
};

export default {
  btn: btn,
  state: state,
  label: label,
  pointTextColor: "text-blue-500",
};
